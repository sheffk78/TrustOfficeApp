[phases.setup]
nixPkgs = ["nodejs-18_x", "npm-9_x"]

[phases.install]
cmds = ["echo 'Using pre-built bundle'"]

[phases.build]
cmds = ["echo 'Skipping build - using pre-built bundle'"]

[start]
cmd = "npx serve -s build -l ${PORT:-3000}"
