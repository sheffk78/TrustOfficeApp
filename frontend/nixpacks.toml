[phases.setup]
nixPkgs = ["nodejs_22", "npm_10"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx serve -s build -l ${PORT:-3000} -c build/serve.json"
