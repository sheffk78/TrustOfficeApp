{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "rm -rf build && npm run build"
  },
  "deploy": {
    "startCommand": "npx serve -s build -l ${PORT:-3000}"
  }
}