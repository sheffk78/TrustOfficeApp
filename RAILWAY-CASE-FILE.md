# Railway Deployment Case File — TrustOfficeApp
**Generated:** 2026-05-03  
**Status:** CRITICAL — ALL deployments on ALL services FAILED. Site may be running on stale container.
**Goal:** Diagnose root cause, propose fix, restore reliable continuous deployment.

---

## 1. Architecture Overview

### Repo: `https://github.com/sheffk78/TrustOfficeApp`
Monorepo with:
```
├── backend/
│   ├── Dockerfile           → Python 3.11-slim, uvicorn server:app --port ${PORT:-8000}
│   ├── requirements.txt     → FastAPI, uvicorn, pymongo, pydantic, etc.
│   ├── server.py            → FastAPI entrypoint
│   └── ... (20+ other .py files)
├── frontend/
│   ├── Dockerfile           → node:20-alpine → nginx:alpine, CRA build
│   ├── railway.toml         → builder="DOCKERFILE", dockerfilePath="Dockerfile"
│   ├── nginx.conf           → SPA routing, security headers, healthcheck /health
│   ├── start.sh             → Substitutes $PORT into nginx config
│   ├── package.json         → React 19, CRACO, Tailwind, Radix UI, react-router-dom 7
│   ├── yarn.lock            → Committed
│   └── build/               → Generated by `yarn build`
└── ... (root-level files like README.md, .gitignore, etc.)
```

### Current Service Layout (Railway Project: `TrustOffice`, ID: `53386002-f1ff-4462-8162-b063b5d0a8e8`)

| Service ID | Name | Status | Purpose |
|---|---|---|---|
| `0f3dd7b7-eef6-4a3f-b1cb-8992a30daaec` | **TrustOfficeApp** | **FAILED (1 deploy)** | **Main app service** |
| `257e060f-22c2-46ee-81cd-7c584eada5a9` | **frontend** | **ALL FAILED** (10 deploys) | **Stuck using NIXPACKS** |
| `c361cdd3-fd37-4f19-b5f0-95a0df01c931` | backend | ALL FAILED (3 deploys) | Python backend |
| `c4cfad38-e4c7-456b-9a85-4bcbbdd41b73` | MongoDB | SUCCESS | Managed DB |
| `6feb29be-b101-4228-bd8c-2a8ae2dbbbc3` | bubbly-generosity | FAILED | Unknown auto-created |
| `8e622c38-4bf7-44e3-a68f-c3090bdb6670` | daring-vibrancy | FAILED | Unknown auto-created |
| `a307f023-243e-4878-b95e-c99fd5336068` | disciplined-harmony | FAILED | Unknown auto-created |

### Environment ID: `7a15a46a-ce5c-497f-a0a6-02f9c6c19d85`

---

## 2. Failed Deployment History (Key Events)

### Phase 1: Frontend Service — April 19-20, 2026
**ALL 10 deployments FAILED.** Manifest showed:
- Builder: `NIXPACKS` (not Docker)
- startCommand: `cd backend && uvicorn server:app` (WRONG — this is on frontend service!)
- rootDirectory: `null`
- Error: `lstat ...snapshot-target-unpack/backend: no such file or directory`

**Jeff tried:** root railway.toml → frontend railway.toml → nixpacks config → dashboard edits → all FAILED.

### Phase 2: TrustOfficeApp Service — May 2, 2026
**1 FAILED deployment.**
Build log: Railpack can't determine how to build. Scans repo root but finds no Dockerfile at root and no `rootDirectory` config tells it to look in `frontend/`.

### Current Situation (May 3, 12:40 PM MT)
- `app.trustoffice.app` returns HTTP 200 — STALE CONTAINER (no SUCCESS deployments)
- `Last-Modified: Fri, 02 May 2026 17:50:15 GMT`
- ALL services except MongoDB have only FAILED deployments
- The running build is from a zombie container

---

## 3. The Live Site Paradox

app.trustoffice.app is serving content but there are ZERO successful deployments in the current Railway state. The site survived because Railway kept an old container running. If it restarts (crash, scale up, healthcheck failure), it will NOT recover.

---

## 4. What Was Attempted

1. Added `frontend/railway.toml` with `builder = "DOCKERFILE"` → committed, pushed
2. Railway still used NIXPACKS (manifest showed `builder: "NIXPACKS"`)
3. Tried GraphQL `serviceInstanceUpdate` to set `rootDirectory: "frontend"`
4. Tried changing builder to `RAILPACK` (manifest showed `builder: "NIXPACKS"` — old deployments)
5. Shifted focus to `TrustOfficeApp` service

---

## 5. Auth & Constraints

- **CLI v4.44+:** DEAD. Do not use for automation.
- **GraphQL API:** WORKS. Token at `~/.hermes/secrets/railway-token.txt`
- **Token owner:** `jeff@socialize.video` (Account API token, full access)
- **Builder options:** `RAILPACK` (auto-detects Dockerfile if at build root)
- **railway.toml behavior:** Overrides dashboard. Must be committed + pushed.
- **ROOT_DIR:** For `frontend/` subdirectory, must set `rootDirectory: "frontend"`

---

## 6. Key Configuration Files

### frontend/railway.toml (most recent):
```toml
[build]
builder = "RAILPACK"
dockerfilePath = "Dockerfile"
```

### frontend/Dockerfile:
- Multi-stage: node:20-alpine → nginx:alpine
- Uses `$REACT_APP_BACKEND_URL` and `$REACT_APP_GOOGLE_CLIENT_ID` as ARG/ENV
- `start.sh` substitutes `$PORT` into nginx config
- nginx listens on `$PORT`, serves SPA

### frontend/start.sh:
```sh
#!/bin/sh
sed -i "s/listen 80/listen ${PORT:-80}/" /etc/nginx/conf.d/default.conf
nginx -g "daemon off;"
```

### backend/Dockerfile:
- Python 3.11-slim, uvicorn on `${PORT:-8000}`

---

## 7. Theories

**Theory 1:** TrustOfficeApp service has `rootDirectory = null`. Railpack scans repo root → finds `./` with backend/ and frontend/ subdirs → can't determine what to build → fails.

**Theory 2:** Even with `rootDirectory = "frontend"`, the `root railway.toml` (if any) overrides the subdirectory one, or the service-level config overrides the file config.

**Theory 3:** The `frontend` service was the original but got permanently stuck in a bad config state (backend startCommand, NIXPACKS builder, null rootDirectory). It should be deleted and recreated.

**Theory 4:** Railway.toml at `frontend/railway.toml` is invisible because the service doesn't know to look for config files in subdirectories. Railway only looks for `railway.toml` at the build root.

**Theory 5:** The repo has conflicting files at root level (Python scripts, `backend_test.py`) that confuse Railpack into thinking it's a Python app, even with `rootDirectory = "frontend"`.

---

## 8. Access Provided

- **GitHub:** `https://github.com/sheffk78/TrustOfficeApp`
- **Railway Token:** Read from `~/.hermes/secrets/railway-token.txt`
- **Railway API:** `https://backboard.railway.app/graphql/v2`
- **Project ID:** `53386002-f1ff-4462-8162-b063b5d0a8e8`
- **Env ID:** `7a15a46a-ce5c-497f-a0a6-02f9c6c19d85`
- **Domain:** `app.trustoffice.app` → Railway (via Cloudflare CNAME)

---

## 9. What We Need

**From GPT 5.5 + Deepseek:**
1. Rank the 3 most likely root causes
2. Propose the exact fix protocol (step-by-step GraphQL calls + git commits)
3. Include a rollback plan if it fails
4. Identify which service is actually serving traffic and what happens if container restarts
5. Propose a clean architectural fix (delete + recreate vs. repair in place)

**Deliverable:** A ranked diagnosis + exact fix protocol.
